#include "main.h"

/**
 * add - Entry point.
 * @num1: the first addend.
 * @num2: the second addend.
 * Return: the sum of @num1 and @num2.
 */

int add(int num1, int num2)
{
	return ((num1 + num2));
}
